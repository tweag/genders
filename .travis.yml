language: haskell

# XXX Disabling until
# https://github.com/travis-ci/travis-ci/issues/4179 is resolved.
# sudo: false
# cache:
#   directories:
#   - $HOME/.stack/
# 
# addons:
#   apt:
#     packages:
#     - libgenders0-dev

before_install:
- mkdir -p ~/.local/bin
- export PATH=~/.local/bin:$PATH
- travis_retry curl -L https://github.com/commercialhaskell/stack/releases/download/v0.1.1.0/stack-0.1.1.0-x86_64-linux.gz | gunzip > ~/.local/bin/stack
- chmod a+x ~/.local/bin/stack
- sudo apt-get install libgenders0-dev

install:
  - stack setup --no-terminal
  - stack build --only-snapshot --no-terminal

script:
  - stack build --no-terminal
